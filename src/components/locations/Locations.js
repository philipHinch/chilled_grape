//components
import MapBox from "./MapBox";

const Locations = () => {
    return (
        <section className="locationsContainer">
            <MapBox />
        </section>
    );
}

export default Locations;